求字符串子串：  s.substring(left, right); 相当于python里面 s[left:right]; 
数组排序（改变了原数组t1)  Arrays.sort(t1);
选一段排序   Arrays.sort ( t1, 1, 5 );
降序排列：  Arrays.sort(t1, Collections.reverseOrder( ));
array 变 string (返回一个新的string )：Arrays.toString(arr)；
